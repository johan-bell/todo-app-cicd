# Consumed by docker-compose.yml. Copy to `.env` for local `docker compose up`,
# or let the deploy workflow generate it from GitHub Actions secrets on the VM.
# NEVER commit the real .env.
POSTGRES_USER=todo
POSTGRES_PASSWORD=change-me
POSTGRES_DB=todo
